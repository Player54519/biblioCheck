git add .
git commit -m "Dernières modifications"
git branch -M main

==> Après modifications, pour les enregistrer/ mettre à jour le github

git branch ==> Si pas bonne url, modifier avec :
git remote set-url origin https://github.com/Player54519/biblioCheck.git


git push -f origin main ==> Pour forcer màj sur github (écraser dépot github avec dépot local)
